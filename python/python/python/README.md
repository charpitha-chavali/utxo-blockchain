# UTXO Blockchain — Python implementation

A Python implementation of a UTXO-based blockchain (the same accounting
model Bitcoin uses), with real elliptic-curve signatures, mining rewards,
and double-spend protection.

## Setup
